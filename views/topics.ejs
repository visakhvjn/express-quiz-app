<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Quiz Topics - Quiz App</title>
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
		<link
			href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap"
			rel="stylesheet"
		/>
		<style>
			:root {
				--primary: #2563eb;
				--primary-hover: #1d4ed8;
				--background: #f8fafc;
				--card-bg: #ffffff;
				--text: #1e293b;
			}

			body {
				min-height: 100vh;
				font-family: 'Inter', sans-serif;
				background-color: var(--background);
				color: var(--text);
				padding-top: 70px;
			}

			.topic-pill {
				background: var(--card-bg);
				border-radius: 50px;
				padding: 8px 20px;
				transition: all 0.2s ease;
				border: 1px solid #e2e8f0;
				text-align: center;
				display: inline-block;
				margin: 4px;
				color: var(--text);
				text-decoration: none;
			}

			.topic-pill:hover {
				background: var(--primary);
				color: white;
				transform: translateY(-2px);
				box-shadow: 0 4px 12px rgba(37, 99, 235, 0.2);
				text-decoration: none !important;
			}

			.topics-container {
				display: flex;
				flex-wrap: wrap;
				justify-content: center;
				gap: 6px;
				padding: 10px;
			}

			.navbar {
				background: var(--card-bg);
				box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1);
			}

			.create-topic-btn {
				background: var(--primary);
				color: white;
				border: none;
				padding: 0.75rem 1.5rem;
				border-radius: 8px;
				cursor: pointer;
				font-size: 1rem;
				font-weight: 500;
				text-decoration: none;
				transition: all 0.2s ease;
			}

			.create-topic-btn:hover {
				background: var(--primary-hover);
				color: white;
				transform: translateY(-1px);
				text-decoration: none;
			}

			a {
				text-decoration: none;
			}
		</style>
	</head>
	<body>
		<nav class="navbar navbar-expand-lg fixed-top">
			<div class="container-fluid">
				<a href="/"><span class="navbar-brand fw-semibold">Quiz App</span></a>
				<a href="/topics/create" class="create-topic-btn ms-auto"
					>Create Topic</a
				>
			</div>
		</nav>

		<div class="container py-4">
			<h1 class="mb-4 text-center">Quiz Topics</h1>
			<p class="text-center mb-4">
				Select a topic to start the quiz or click on create to make a new one
			</p>

			<div class="topics-container">
				<% topics.forEach(topic => { %>
				<a href="/question/<%= topic.topic %>" class="topic-pill">
					<%= topic.topic %>
				</a>
				<% }) %>
			</div>
		</div>

		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
	</body>
</html>
